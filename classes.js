const catalog = {
    remote,
    children: [],
}
const location = {
    id,
    name,
    parent,
    children
}

const file = {
    id,
    name,
    parent,
    type,
    meta,
}

const collection = {
    id,
    name,
    locations: []
}